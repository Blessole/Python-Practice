# 자료형
    - 변수를 만들 때 사용자가 자료형을 결정하지 않아도 파이썬 내부에서 자동으로 판단 후 적용
    - 자료형 확인은 type() 함수로 확인
    - 필요에 따라 내가 자료형을 변경할 수 있음

    * 숫자형
        - 파이썬에서의 4칙 연산
            a + b = a 더하기 b
            a - b = a 빼기 b
            a * b = a 곱하기 b
            a / b = a 나누기 b                  > int와 int를 나누더라도 출력 값은 float로 나옴
            a // b = a를 b로 나눈 몫          ★ 자주 쓰임
            a % b = a를 b로 나눈 나머지     ★ 자주 쓰임
            a ** b = a의 b 제곱                 ★ 자주 쓰임

        > 참고 규칙
            - 연산 변수 중 하나라도 float이 있으면 결과 값은 무조건 float!

    * 논리형
        - bool : true(참), false(거짓)
        
        > 연산자
            - 비교 연산자 : <, <=, >, >=, ==, !=
            - 논리 연산자 : or, and, not

    * 문자열형
        - str (Java와 달리 char와 String을 따로 구분하지 않음!)
        - ' ', " " : 둘 다 사용 가능!
        - 연산 불가 하지만, 예외적으로 '문자+문자', '문자*정수' 는 가능
            ex1) str + str : 문자 두개를 붙임
                    a = '5'
                    print(a + a) : 55
            ex2) int * str : 문자를 int 개수 만큼 이어붙여 출력함
                    a = '5'
                    b = 5.0
                    print(a * b) : 55555
    
    * 군집 자료형 : 여러 데이터를 모은 집합 형태 자료형
        - list  : 순서 있고, 값 변경 되고, 중복 허용 되는 열거형 집합
        - tuple : 순서가 있고, 값을 변경할 수 없는 열거형 집합
        - set   : 순서가 없고, 중복 허용 안되는 집합
        - dictionary : 키와 값의 쌍으로 구성된 집합

    * 자료형 변환 방법
        - 원하는 자료형 함수에 값을 넣기!
            ex) str(10) : int인 10을 문자열로 변경
                  int('10') : str인 '10'을 int로 변경
                  int(12.5) : float인 12.5를 int로 변경
                  list('hello') : str인 'hello'를 list로 변경

* 연산자 우선순위
산술 연산자 > 관계 연산자 > 논리 연산자